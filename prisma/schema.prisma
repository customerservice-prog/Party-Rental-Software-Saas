generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Core schema omitted in this update is intentionally not safe to reconstruct.
// This commit is aborted/replaced by a complete schema update in the next commit.
