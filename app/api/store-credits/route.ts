import {NextRequest,NextResponse} from "next/server";
import {randomUUID} from "crypto";
import {prisma} from "@/lib/prisma";
import {requireCurrentOrganization} from "@/lib/tenant";
import {requirePermission,authzErrorResponse} from "@/lib/authz";

type CreditRow={id:string;organizationId:string;customerId:string;sourceOrderId:string|null;type:string;originalAmount:number;remainingAmount:number;reason:string|null;expiresAt:Date|null;status:string;createdBy:string|null;createdAt:Date;updatedAt:Date};
async function customerExists(orgId:string,id:string){return !!(await prisma.customer.findFirst({where:{id,organizationId:orgId},select:{id:true}}))}
export async function GET(req:NextRequest){const org=await requireCurrentOrganization();try{await requirePermission(org.id,"customers.view")}catch(e){return authzErrorResponse(e)}const customerId=new URL(req.url).searchParams.get("customerId");if(!customerId)return NextResponse.json({error:"customerId is required"},{status:400});if(!await customerExists(org.id,customerId))return NextResponse.json({error:"Customer not found"},{status:404});const credits=await prisma.$queryRawUnsafe<CreditRow[]>(`SELECT * FROM "StoreCredit" WHERE "organizationId"=$1 AND "customerId"=$2 ORDER BY "createdAt" DESC`,org.id,customerId);const transactions=await prisma.$queryRawUnsafe<any[]>(`SELECT * FROM "StoreCreditTransaction" WHERE "organizationId"=$1 AND "customerId"=$2 ORDER BY "createdAt" DESC LIMIT 100`,org.id,customerId);return NextResponse.json({credits,transactions})}
export async function POST(req:NextRequest){const org=await requireCurrentOrganization();let user;try{user=await requirePermission(org.id,"orders.manage")}catch(e){return authzErrorResponse(e)}const b=await req.json(),customerId=String(b.customerId||"");if(!customerId||!await customerExists(org.id,customerId))return NextResponse.json({error:"Customer not found"},{status:404});const action=String(b.action||"issue");if(action==="issue"){const amount=Number(b.amount);if(!Number.isFinite(amount)||amount<=0)return NextResponse.json({error:"Amount must be greater than zero"},{status:400});const type=b.type==="rain_check"?"rain_check":"store_credit",id=randomUUID();let sourceOrderId:string|null=null;if(b.sourceOrderId){const o=await prisma.order.findFirst({where:{id:String(b.sourceOrderId),organizationId:org.id,customerId},select:{id:true}});if(!o)return NextResponse.json({error:"Source order not found for this customer"},{status:404});sourceOrderId=o.id}const expiresAt=b.expiresAt?new Date(String(b.expiresAt)):null;await prisma.$executeRawUnsafe(`INSERT INTO "StoreCredit" ("id","organizationId","customerId","sourceOrderId","type","originalAmount","remainingAmount","reason","expiresAt","status","createdBy") VALUES ($1,$2,$3,$4,$5,$6,$6,$7,$8,'active',$9)`,id,org.id,customerId,sourceOrderId,type,amount,b.reason?String(b.reason):null,expiresAt,user.id);await prisma.$executeRawUnsafe(`INSERT INTO "StoreCreditTransaction" ("id","organizationId","creditId","customerId","orderId","type","amount","notes","performedBy") VALUES ($1,$2,$3,$4,$5,'issue',$6,$7,$8)`,randomUUID(),org.id,id,customerId,sourceOrderId,amount,b.reason?String(b.reason):null,user.id);return NextResponse.json({success:true,id},{status:201})}if(action==="redeem"){const creditId=String(b.creditId||""),amount=Number(b.amount),orderId=b.orderId?String(b.orderId):null;if(!Number.isFinite(amount)||amount<=0)return NextResponse.json({error:"Amount must be greater than zero"},{status:400});const rows=await prisma.$queryRawUnsafe<CreditRow[]>(`SELECT * FROM "StoreCredit" WHERE "id"=$1 AND "organizationId"=$2 AND "customerId"=$3 LIMIT 1`,creditId,org.id,customerId);const c=rows[0];if(!c)return NextResponse.json({error:"Credit not found"},{status:404});if(c.status!=="active"||c.remainingAmount<amount)return NextResponse.json({error:"Insufficient active credit"},{status:400});if(c.expiresAt&&new Date(c.expiresAt)<new Date())return NextResponse.json({error:"Credit has expired"},{status:400});if(orderId){const o=await prisma.order.findFirst({where:{id:orderId,organizationId:org.id,customerId},select:{id:true}});if(!o)return NextResponse.json({error:"Order not found for this customer"},{status:404})}const remaining=c.remainingAmount-amount,status=remaining<=0.0001?"redeemed":"active";await prisma.$executeRawUnsafe(`UPDATE "StoreCredit" SET "remainingAmount"=$1,"status"=$2,"updatedAt"=CURRENT_TIMESTAMP WHERE "id"=$3 AND "organizationId"=$4`,remaining,status,creditId,org.id);await prisma.$executeRawUnsafe(`INSERT INTO "StoreCreditTransaction" ("id","organizationId","creditId","customerId","orderId","type","amount","notes","performedBy") VALUES ($1,$2,$3,$4,$5,'redeem',$6,$7,$8)`,randomUUID(),org.id,creditId,customerId,orderId,amount,b.notes?String(b.notes):null,user.id);return NextResponse.json({success:true,remainingAmount:remaining})}return NextResponse.json({error:"Invalid action"},{status:400})}
