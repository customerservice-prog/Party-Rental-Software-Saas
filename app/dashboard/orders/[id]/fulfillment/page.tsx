import Link from "next/link";
import {notFound} from "next/navigation";
import {requireCurrentOrganization} from "@/lib/tenant";
import {prisma} from "@/lib/prisma";
import FulfillmentBoard from "./FulfillmentBoard";
export default async function FulfillmentPage({params: paramsPromise}:{params:Promise<{id:string}>}){
  const params = await paramsPromise;
const org=await requireCurrentOrganization();const order=await prisma.order.findFirst({where:{id:params.id,organizationId:org.id},include:{customer:true,items:{include:{item:true}},deliveryDriver:true,pickupDriver:true}});if(!order)notFound();return <div className="space-y-5 pb-10"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><Link href={`/dashboard/orders/${order.id}`} className="text-xs font-bold text-blue-600">← Order {order.orderNumber}</Link><div className="mt-2 text-xs font-black uppercase tracking-[.16em] text-blue-600">Fulfillment</div><h1 className="mt-1 text-3xl font-black tracking-tight">Load, deliver & return</h1><p className="mt-1 text-sm text-slate-500">{order.customer.firstName} {order.customer.lastName} · {new Date(order.eventDate).toLocaleDateString()}</p></div><div className="flex flex-wrap gap-2"><Link href={`/dashboard/warehouse/orders/${order.id}`} className="rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-black text-white">Scan this order</Link><Link href="/dashboard/warehouse" className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-black">Asset registry</Link><Link href="/dashboard/deliveries" className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-black">Delivery board</Link></div></div><FulfillmentBoard orderId={order.id} orderNumber={order.orderNumber} initialItems={order.items.map(x=>({orderItemId:x.id,itemName:x.item.name,expectedQty:x.quantity}))}/></div>}
